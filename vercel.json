{
  "version": 2,
  "builds": [
    {
      "src": "frontend/cd app/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "frontend/cd app/dist" }
    },
    {
      "src": "src/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "src/index.js" },
    { "src": "/(.*)", "dest": "frontend/cd app/dist/$1" }
  ]
}
